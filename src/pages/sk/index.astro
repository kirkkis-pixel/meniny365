---
import Layout from '../../layouts/Layout.astro';
import { getTodayString, formatDateSlovak } from '../../lib/date';
import { getNamesByDate } from '../../lib/meniny';
import { toSlug } from '../../lib/slug';

// Get today's date and names
const today = getTodayString();
const todayNames = await getNamesByDate(today);
const todayFormatted = formatDateSlovak(today);

// Generate structured data
const structuredData = {
  "@context": "https://schema.org",
  "@type": "WebPage",
  "name": `Kto má dnes meniny - ${todayFormatted}`,
  "description": `Dnes ${todayFormatted} majú meniny: ${todayNames.join(', ')}`,
  "url": `https://meniny365.sk/sk`,
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": todayNames.map((name, index) => ({
      "@type": "ListItem",
      "position": index + 1,
      "name": name,
      "url": `https://meniny365.sk/sk/meno/${toSlug(name)}`
    }))
  }
};
---

<Layout 
  title={`Kto má dnes meniny - ${todayFormatted} | meniny365`}
  description={`Dnes ${todayFormatted} majú meniny: ${todayNames.join(', ')}. Zistite kto oslavuje meniny, nájdite darčekové nápady a viac informácií.`}
  canonical="https://meniny365.sk/sk"
  structuredData={structuredData}
>
  <!-- Apple-inspired Hero Section -->
  <section class="relative overflow-hidden">
    <!-- Background gradient -->
    <div class="absolute inset-0 bg-gradient-to-br from-blue-50 via-white to-purple-50 dark:from-gray-900 dark:via-black dark:to-purple-900"></div>
    
    <div class="relative container py-24 md:py-32">
      <div class="text-center max-w-4xl mx-auto">
        <!-- Main heading with Apple-style typography -->
        <h1 class="text-display mb-6 animate-fade-in-up">
          Kto má dnes meniny
        </h1>
        
        <!-- Date with subtle styling -->
        <p class="text-subtitle mb-12 animate-fade-in-up" style="animation-delay: 0.1s">
          {todayFormatted}
        </p>
        
        <!-- Names display with Apple-style cards -->
        {todayNames.length > 0 ? (
          <div class="card p-12 max-w-3xl mx-auto mb-16 animate-fade-in-scale" style="animation-delay: 0.2s">
            <h2 class="text-title mb-8 text-[var(--text)]">Dnes oslavujú meniny</h2>
            <div class="flex flex-wrap justify-center gap-4">
              {todayNames.map((name, index) => (
                <a 
                  href={`/sk/meno/${toSlug(name)}`}
                  class="px-8 py-4 bg-gradient-to-r from-blue-500 to-purple-600 text-white rounded-2xl font-semibold text-xl shadow-lg hover:shadow-xl transition-all duration-300 hover:scale-105 animate-fade-in-up" 
                  style={`animation-delay: ${0.3 + index * 0.1}s`}
                >
                  {name}
                </a>
              ))}
            </div>
            
            <!-- iCal download button -->
            <div class="mt-8">
              <a 
                href={`/api/ical?date=${today}`}
                class="btn btn-secondary text-lg px-8 py-4"
                download={`meniny-${today}.ics`}
              >
                📅 Pridať do kalendára
              </a>
            </div>
          </div>
        ) : (
          <div class="card p-12 max-w-3xl mx-auto mb-16 animate-fade-in-scale" style="animation-delay: 0.2s">
            <div class="text-center">
              <div class="w-16 h-16 bg-gradient-to-br from-gray-400 to-gray-500 rounded-full flex items-center justify-center mx-auto mb-6">
                <span class="text-white text-2xl">📅</span>
              </div>
              <p class="text-body text-[var(--text-secondary)]">
                Dnes nemá nikto meniny. Skúste pozrieť iný deň v kalendári.
              </p>
            </div>
          </div>
        )}

        <!-- Navigation buttons with Apple-style design -->
        <div class="flex flex-col sm:flex-row justify-center gap-4 mb-20 animate-fade-in-up" style="animation-delay: 0.4s">
          <a href="/sk/kalendár" class="btn btn-primary text-lg px-8 py-4">
            📅 Kalendár menín
          </a>
          <a href="/sk/populárne-mená" class="btn btn-secondary text-lg px-8 py-4">
            🔥 Populárne mená
          </a>
          <a href="/sk/návrhy-mena" class="btn btn-ghost text-lg px-8 py-4">
            💡 Návrhy mien
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Apple-inspired Content Section -->
  <section class="py-20 bg-gradient-to-b from-transparent to-[var(--accent)]">
    <div class="container">
      <div class="max-w-4xl mx-auto text-center">
        <h2 class="text-headline mb-8 text-[var(--text)]">Meniny na Slovensku</h2>
        <p class="text-body mb-12 leading-relaxed">
          Meniny sú dôležitou súčasťou slovenskej kultúry a tradície. Každý deň v roku má svoje mená, 
          ktoré sa oslavujú namiesto narodenín. 
          {todayNames.length > 0 ? `Dnes ${todayFormatted} majú meniny ${todayNames.join(', ')}. ` : ''}
          Na našej stránke nájdete kompletný kalendár menín, darčekové nápady, pôvod mien a ďalšie užitočné informácie.
        </p>
        
        <div class="grid md:grid-cols-2 gap-12 text-left">
          <div class="card p-8">
            <h3 class="text-title mb-4 text-[var(--text)]">Ako fungujú meniny?</h3>
            <p class="text-body leading-relaxed">
              Meniny sú tradične oslavované namiesto narodenín na Slovensku. Každý deň v roku má priradené 
              konkrétne mená, ktoré sa môžu líšiť v závislosti od regiónu. Tieto mená sú často spojené s 
              kresťanskými svätými alebo historickými osobnostami.
            </p>
          </div>
          
          <div class="card p-8">
            <h3 class="text-title mb-4 text-[var(--text)]">Darčekové nápady</h3>
            <p class="text-body leading-relaxed">
              Hľadáte darček na meniny? Na našej stránke nájdete množstvo nápadov pre každý rozpočet a príležitosť. 
              Od tradičných darčekov po moderné trendy - máme pre vás pripravené tipy pre každého.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Apple-inspired Quick Links -->
  <section class="py-20">
    <div class="container">
      <div class="text-center mb-16">
        <h2 class="text-headline mb-4 text-[var(--text)]">Objavte viac</h2>
        <p class="text-subtitle">Všetko čo potrebujete vedieť o meninách na jednom mieste</p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <a href="/sk/kalendár" class="card p-8 group hover:scale-105 transition-all duration-300 hover:shadow-hover">
          <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <span class="text-white text-2xl">📅</span>
          </div>
          <h3 class="text-title mb-4 text-[var(--text)] group-hover:text-[var(--brand)] transition-colors duration-300">Kalendár menín</h3>
          <p class="text-body">Pozrite si kompletný kalendár menín pre celý rok s krásnym dizajnom</p>
        </a>
        
        <a href="/sk/populárne-mená" class="card p-8 group hover:scale-105 transition-all duration-300 hover:shadow-hover">
          <div class="w-16 h-16 bg-gradient-to-br from-orange-500 to-red-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <span class="text-white text-2xl">🔥</span>
          </div>
          <h3 class="text-title mb-4 text-[var(--text)] group-hover:text-[var(--brand)] transition-colors duration-300">Populárne mená</h3>
          <p class="text-body">Zistite ktoré mená sú najpopulárnejšie na Slovensku</p>
        </a>
        
        <a href="/sk/návrhy-mena" class="card p-8 group hover:scale-105 transition-all duration-300 hover:shadow-hover">
          <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-pink-500 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300">
            <span class="text-white text-2xl">💡</span>
          </div>
          <h3 class="text-title mb-4 text-[var(--text)] group-hover:text-[var(--brand)] transition-colors duration-300">Návrhy mien</h3>
          <p class="text-body">Nájdite perfektné meno pre vaše dieťa s našou databázou</p>
        </a>
      </div>
    </div>
  </section>
</Layout>